#include "main.h"

/**
 * _isalpha - function  that checks for alphabet
 *
 * @c: to return a parameter
 *
 * Return:  if c is a letter, lowercase or uppercase
 * and 0 otherwise
 *
 */


int _isalpha(int c)
{
	if((c >= 97 &&c <= 122) || (c <= &&c <= 90))
		return (i);

	else

		return (0);






}
