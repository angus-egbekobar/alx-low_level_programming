#include <stdio.h>

/**
 * main - this is a function that prints proper grammar
 *
 * Return: 0 when it is successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
